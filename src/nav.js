import React from 'react';
export default function Navbar(){
    return(
        <nav className='navbar'>
            <h2>{role} Dashboard</h2>
            <ul>
                {role} === 'student' 
            </ul>
        </nav>
    );
}